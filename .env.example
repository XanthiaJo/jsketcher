# GitHub webhook secret used to verify push event signatures.
# Generate one with: openssl rand -hex 20
# Set the same value in GitHub repo settings → Webhooks → Add webhook → Secret.
GITHUB_WEBHOOK_SECRET=
